// Placeholder — the real landing page (mission, how-it-works) lands in a
// later commit. This just confirms the scaffold renders end to end.
export default function Home() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center p-24">
      <h1 className="text-3xl font-bold">Canopychain</h1>
      <p className="mt-2 text-gray-500">Milestone-verified reforestation funding on Stellar.</p>
    </main>
  );
}
